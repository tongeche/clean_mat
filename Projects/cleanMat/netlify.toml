[build]
  # remove or comment out the base line entirely
  # base    = "projects/cleanmat"

  # publish from the repo root (where index.html lives)
  publish = "."

  # if you have no build step, you can leave this empty
  command = ""
